package com.weinhold.hexagon.conventions.domain;

/**
 * A value object. Nothing about its name or package says what role it plays, so the
 * conventions must leave it alone rather than pad the core with guesses.
 */
public class Money {

}
